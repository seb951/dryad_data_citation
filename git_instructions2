
git config --global user.name "Yöùr námè" # UTF8, tell your name (git will sign the change)
git config --global user.email "your@mail.com"
git config --global color.ui "auto"  # to change the color of the interface, letting git choose the palette
git config --global core.editor "TextWrangler"  ## or "notepad" or "vim"
git config --list #will list all above configuration changes you have made


#start new git. Just for the initial commitment of the project...
git init 

#commit all the files you want#
git add alignments.R
git add README
git add .gitignore

#always add a README file
touch README

#remove files you don't want up on git anymore.
#git rm 

#see what has changed.
#git status

#tell you diff between files you have, and the ones that have been committed.
#git diff

#show you the changes that have been done
#git log

#git fetch
#to see the status of the remote version vs your.

#git pull
#pull it from the remote version

#go back to the previous version
#git checkout

#add a .gitignore file to ignore certain files or certain extensions...
touch .gitignore

#commit to git with -m option for messaging (without the option will open a text editor for you to modify
git commit -m 'initial commit'

#find (pull) the right repo
#git pull https://github.com/seb951/helianthus_hybrid_species_transcriptome.git ###old version. Does not seem to work anymore (13-08-2013)
git remote add origin https://github.com/seb951/dryad_data_citation.git ###use this line instead

#push files unto github!
#'use git push -u origin master' when pushing for the first time...
git push origin master

#username: seb951
#password: 12coregon
